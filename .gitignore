# --- agentic-suite (auto-managed) — keep plan/docs, ignore run machinery ---
plan/*
!plan/docs/
.agentic-builder/

# --- local git worktrees (scratch, not part of the repo tree) ---
.worktrees/

# --- subagent-driven-development scratch (ledger, briefs, review packages) ---
.superpowers/

# --- monorepo build output / deps / local env ---
**/node_modules/
**/dist/
**/coverage/
**/.env
!**/.env.example
backend/dev.db
backend/*.db-journal
backend/prisma/dev.db
backend/prisma/*.db-journal
backend/uploads/
**/*.log
